{
  "name": "mandate",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": ["apps/web", "cli"],
  "scripts": {
    "test": "anchor test",
    "cli": "npm run start --workspace=cli",
    "web": "npm run dev --workspace=web"
  }
}
